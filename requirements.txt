#    -Python version (e.g., 3.8+)
#    -Psycopg2       (in IDE terminal: pip install psycopg2)
#    -Pillow         (in IDE terminal: pip install pillow)
#   -Tkinter
#    -**PostgreSQL** (para aceder a base de dados)